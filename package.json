{
  "name": "result-monad",
  "version": "1.0.0",
  "description": "extends maybe-monad to add an error type to the monad",
  "main": "index.js",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc",
    "pretest": "npm run build",
    "test": "jasmine",
    "push": "git push origin && git push origin --tags",
    "prestart": "npm run build",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Roaders/result-monad.git"
  },
  "author": "Giles Roadnight",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Roaders/result-monad/issues"
  },
  "homepage": "https://github.com/Roaders/result-monad#readme",
  "keywords": [
    "typescript"
  ],
  "dependencies": {
    "maybe-monad": "^1.1.0"
  },
  "devDependencies": {
    "jasmine": "2.8.0",
    "typescript": "2.6.1",
    "@types/jasmine": "^2.8.2",
    "@types/node": "^8.0.53"
  }
}
